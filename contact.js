function check() {
    var name=document.contact.uname;
    var number = document.contact.uphone;
    var mail=document.contact.umail;
    var message = document.contact.message;
    {
        if(name=="")
        {
            alert("enter the name");
        }
    }
}